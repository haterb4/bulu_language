[package]
name = "test-app"
version = "0.1.0"
authors = ["Test User <test@example.com>"]
description = "Test application"
license = "MIT"

[dependencies]
math-utils = "^1.0.0"

[build]
optimization = "2"
target = "native"
features = []
incremental = false
parallel = false

[test]
parallel = true
coverage = false
