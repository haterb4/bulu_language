func main() {
    println("Test programArgs")
    let a = programArgs()
    println("Length: " + len(a).toString())
    for i in 0..len(a) {
        println("arg[" + i.toString() + "] = " + a[i])
    }
}
