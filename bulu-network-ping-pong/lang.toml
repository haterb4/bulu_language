[package]
name = "bulu-network-ping-pong"
version = "0.1.0"
authors = ["Your Name <your.email@example.com>"]
description = "A Bulu project named bulu-network-ping-pong"

[dependencies]

[build]
optimization = "2"
target = "native"
features = []
incremental = true
parallel = true

[test]
parallel = true
timeout = 30
coverage = false
