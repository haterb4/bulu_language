[package]
name = "math-utils"
version = "2.0.0"
authors = ["Bulu Team <team@bulu-lang.com>"]
description = "Mathematical utility functions with Point and Vector support for Bulu"
keywords = ["math", "utilities", "vector", "geometry"]

[dependencies]

[build]
optimization = "2"
target = "native"
features = []
incremental = true
parallel = true

[test]
parallel = true
timeout = 30
coverage = false
