// Test simple des boucles for avec array

func main() {
    println("=== Test simple des boucles for ===")
    
    // Boucle for avec array
    println("Boucle for avec array:")
    let numbers = [1, 2, 3]
    for num in numbers {
        println("num = " + num.toString())
    }
    
    println("Test terminé")
}